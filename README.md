# selene-project

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run frontend
```

This project was created using expo and using bun 'v1.3.0' and node 'v22.13.1'. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
