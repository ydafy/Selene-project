# ============================================================================
# Selene Frontend — Environment Variables (template)
# ============================================================================
# Copy this file to `.env` and fill the values for your environment.
# All EXPO_PUBLIC_* values are bundled into the client app — never put
# server secrets here.

# --- Supabase (public anon key only) ---
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- Google Sign-In ---
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-web-client-id
EXPO_PUBLIC_GOOGLE_MAPS_KEY=your-google-maps-api-key

# --- Stripe ---
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# --- Legal links (CONF-012 / CONF-013) ---
# Tappable rows in Settings > Legales open these URLs via expo-web-browser.
EXPO_PUBLIC_TERMS_URL=https://selene.mx/terminos
EXPO_PUBLIC_PRIVACY_URL=https://selene.mx/privacidad

# --- Chatwoot support widget (CONF-018) ---
# Both vars are required. If either is missing, the Soporte row shows the
# localized "support.unavailable" message instead of opening Chatwoot.
EXPO_PUBLIC_CHATWOOT_BASE_URL=https://chatwoot.tudominio.com
EXPO_PUBLIC_CHATWOOT_WEBSITE_TOKEN=placeholder-token
